<% @trips.each do |trip| %>
  <tr>
    <td><%= trip.type %></td>
    <td><%= link_to "Edit", edit_trip_path(trip), remote: true, class: "btn btn-default" %></td>
    <td><%= link_to "Delete", trip_delete_path(trip), remote: true, class: "btn btn-danger" %></td>
  </tr>
<% end %>